{
  "name": "fetch-file",
  "version": "1.0.0",
  "description": "Fetch a file only if it is out of date and needing refresh",
  "main": "fetch-file.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rektide",
  "license": "X",
  "dependencies": {
    "bluebird": "^2.3.2",
    "fs-xattr": "^0.1.3",
    "request": "^2.44.0",
    "stream-aggregate": "^0.1.1",
    "xattr-async": "^0.1.3"
  }
}
